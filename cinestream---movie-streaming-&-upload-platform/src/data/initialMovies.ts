import { Movie } from '../types';

export const ADMIN_EMAIL = 'linux4489@gmail.com';

// Empty catalog by default so ONLY user-uploaded videos exist and play
export const INITIAL_MOVIES: Movie[] = [];
